# Select columns
select 'select customerName, city from customer;';
select customerName, city from customer;

# Select everything (all columns)
select 'select * from customer;';
select * from customer;